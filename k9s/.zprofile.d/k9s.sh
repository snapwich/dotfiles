# k9s gates node shell (`s` on a node) per context, in
# ~/.config/k9s/clusters/<cluster>/<context>/config.yaml. this env var turns it
# on globally instead. undocumented, but read by k9s at startup.
export K9S_FEATURE_GATE_NODE_SHELL=true
